var parser = require('./pbxproj')
    fs = require('fs'),
    util = require('util'),
    project = fs.readFileSync('examples/project.pbxproj', 'utf-8'),
    rawProj = parser.parse(project)

var fullLog = function (obj) {
    console.log(util.inspect(obj, true, null))
}

fullLog(rawProj.project)
